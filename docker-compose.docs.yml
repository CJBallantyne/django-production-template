services:
  docs:
    image: django_project_template_local_docs
    container_name: django_project_template_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./django_project_template:/app/django_project_template:z
    ports:
      - '9000:9000'
    command: /start-docs
